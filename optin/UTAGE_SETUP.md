# 天道象元 公式 無料プレゼント オプトインLP — UTAGE設定仕様

天道象元のビジネスへの入口として、見込み顧客の「お名前・メールアドレス」と
「公式LINE登録」をいただくためのオプトインLPです。既に UTAGE 上に一式作成済みです。

## 全体構成（UTAGEファネル）

- ファネルID: `IyYKAOwMMDPC`
- ファネル名: 「天道象元 無料プレゼント オプトインLP（3大特典）」

| ステップ | ページ内容 | 公開URL |
| --- | --- | --- |
| ① LP | `optin/index.html`（本リポジトリ）を raw_html インポート | https://utage-system.com/p/1MJJCOhH8R7u |
| ② 登録フォーム | UTAGEネイティブの「フォーム」要素（お名前・メールアドレス） | https://utage-system.com/p/2P8EUynCjJkQ |
| ③ サンキュー・LINE登録 | line-button / line-qr で公式LINE登録を案内 | https://utage-system.com/p/1oPYvFaF9gna |

**登録導線**: ① LPのCTAボタン → ② 名前・メール登録（送信後、自動で③にリダイレクト）→
③ 公式LINEにも登録 → 特典1・2はメールで、特典3はLINE登録後にその場で案内。

## メッセージ配信（配信アカウント／シナリオ）

- 配信アカウントID: `ZOnJq5rXbzVt`「天道象元 公式 無料プレゼント（3大特典）シナリオ」（種別: mail_line）
- シナリオID: `F8qU28obaH2D`「3大特典 オプトイン シナリオ」
- ラベル: `gDa1LjfEDQ05`「オプトインLP登録者（3大特典）」（作成のみ。読者への自動付与は未設定 — 下記「残作業」参照）
- ステップメール: `Hym6q9UuhMHC`「STEP0: ご登録直後（特典1・2のお届け）」
  - **ステータス: draft（下書き）** — 実URLを入力し `reserved` に変更するまで配信されません。安全に確認いただけます。

## 特典（3つ）

1. **電子書籍(PDF)全文プレゼント**『成功者だけがやっている 運を引き寄せる27の習慣』
   （添付いただいた書籍表紙画像を使用）— メールでPDFをお届け
2. **天道象元 特別解説動画**「決断力を高める 天命氣学ダイジェスト」— メールで視聴URLをお届け
   （バナー画像は生成AI・Gammaで作成。下記「残作業」参照）
3. **「本質・決断力 自己診断チェックシート」(PDF)** — 公式LINE登録者限定でその場提供

## 画像素材

ページ内の画像は、既にUTAGEのメディアライブラリに保存済みの以下のファイルを再利用しています
（`consult-*` として今回のセッション内で別funnel用に登録済みだったものと同一の素材のため共有）。

| 用途 | URL |
| --- | --- |
| ロゴ（ベスト・フューチャー） | `https://utagesystem.s3.ap-northeast-1.wasabisys.com/zRDT5xeAoSsc/4GYEjuPfUt32.png` |
| 天道象元 プロフィール写真 | `https://utagesystem.s3.ap-northeast-1.wasabisys.com/zRDT5xeAoSsc/pZsUQu0UCaUZ.jpg` |
| 書籍表紙（27の習慣） | `https://utagesystem.s3.ap-northeast-1.wasabisys.com/zRDT5xeAoSsc/N7xKaJUY9GSc.jpg` |
| お客様の声 ×3 | `.../JEAuQWxBts1J.jpg`, `.../zOZkIKNDgXqh.jpg`, `.../z7CgI1lFcvmU.jpg` |

特典2のバナー画像のみ、生成AI（Gamma）で新規作成した外部CDN画像を直接参照しています
（`https://cdn.gamma.app/.../QeDVThmCA4gFmYb6B6kMA.jpg`）。この実行環境からはUTAGEの
メディアライブラリへ直接アップロードできなかったため、外部URLを暫定利用しています。
表示は正常に確認できていますが、恒久的にはUTAGEのメディアライブラリへ移すことをおすすめします
（下記「残作業」参照）。

特典3は具体的な絵柄がないため、画像の代わりにチェックリスト風のアイコン（SVG）で表現しています。

## 残作業（ユーザー側で対応いただく項目）

このLPは仕組みとして完成していますが、実際に配信を開始する前に以下をご確認・ご用意ください。

1. **書籍PDFの実ファイル**
   `成功者だけがやっている 運を引き寄せる27の習慣` のPDF原稿を共有いただければ、
   UTAGEのメディアライブラリにアップロードし、ステップメール本文のURLを差し替えます。
2. **特典2の解説動画**
   実際の動画（またはスライド等）をご用意いただければ、視聴URLをステップメールに設定します。
   併せて、バナー画像もUTAGEメディアライブラリへ本アップロードします。
3. **特典3の自己診断シートPDF**
   実際のPDFファイルをご用意いただければ、公式LINEの自動応答／サンキューページの案内文に
   ダウンロードURLとして設定します（現在はプレースホルダーです）。
4. **ステップメールの有効化**
   `Hym6q9UuhMHC` の本文プレースホルダー（`[ここに...URLを設定してください]`）を実URLに差し替えた上で、
   ステータスを `draft` → `reserved` に変更してください（UTAGE管理画面、または本APIから変更可能です）。
5. **公式LINEの接続確認**
   ③サンキューページの line-button / line-qr が、実際の公式LINEアカウント（@soy9097d）に
   正しくつながるか、UTAGE管理画面のプレビューで一度ご確認ください
   （本セッションのネットワーク制限により、実ページの目視確認ができていません）。
6. **フッターのリンク**
   LPフッターの「プライバシーポリシー」「特定商取引法に基づく表記」は `#`（仮リンク）のままです。
   実際のURLに差し替えてください。
7. **ラベルの自動付与（任意）**
   `gDa1LjfEDQ05` ラベルを登録者に自動付与したい場合、UTAGE管理画面の
   「配信アクション」機能で登録フォームの送信アクションに追加してください
   （本APIからの紐付けは `action_id` のバリデーションの都合上、今回は見送りました）。

## ローカルファイル

- `optin/index.html` … LPのソース（画像は `optin/images/` への相対パス。ローカルプレビュー用）
- `optin/images/` … LPで使用する画像（book-27.jpg / tendou-shogen.jpg / logo.png / voice-0X.jpg）
- UTAGEへ再インポートする際は、`images/xxx.jpg` を上記の `https://utagesystem.s3...` URLに
  置き換えたバージョンをアップロードしてください（今回はこの変換を行った上でインポート済みです）。
